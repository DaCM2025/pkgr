# pkgr

This package was developped live in class
to showcase different features of R package development.

Try to install it on your computre using:
```r
## install.packages("remotes") # if you don't have remotes installed
remotes::install_github("DaCM2025/pkgr")
```

Then load it using:
```r
library(pkgr)
```
